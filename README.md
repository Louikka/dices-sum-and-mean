dices-sum-and-mean
---

This script simulates dice(s) throws (or just randomizes numbers) and calculates average sum and mean of thrown dice(s).

Usage: `$ ruby main.rb [options]` (`-h` or `--help` for help message).
